# Financeiro Frontend

Painel operacional do core financeiro, seguindo a linguagem visual da `Escola`.

## Como rodar

1. Rode `npm install`
2. Configure `NEXT_PUBLIC_FINANCEIRO_API_URL` se quiser sobrescrever `http://localhost:3002/api/v1`
3. Rode `npm run dev`
